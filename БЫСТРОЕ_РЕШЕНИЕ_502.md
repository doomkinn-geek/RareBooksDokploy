# 🚀 Быстрое решение проблемы 502 Bad Gateway

## ❌ Проблема
После `docker compose up -d --build` появляется ошибка 502 Bad Gateway, которая исчезает только после `docker compose restart`.

## ✅ Решение
Все исправлено! Теперь можно просто выполнить:

```bash
docker compose up -d --build
```

И сайт будет работать **сразу** без дополнительных действий.

## 🔧 Что было исправлено

### В docker-compose.yml:
- ✅ Увеличены периоды инициализации (start_period)
- ✅ Оптимизированы healthcheck параметры
- ✅ Добавлена устойчивость к временным сбоям

### В nginx/nginx_prod.conf:
- ✅ Уменьшено время кэширования DNS (30s → 10s)
- ✅ Добавлена автоматическая retry логика при ошибках 502/503/504
- ✅ Настроены max_fails и fail_timeout для upstream'ов

## ⏱️ Время запуска

После команды `docker compose up -d --build` подождите:
- **Backend сервисы**: ~2-3 минуты для инициализации
- **Frontend сервисы**: ~1-2 минуты
- **Nginx**: запустится автоматически когда все готово

## 🧪 Проверка работы

```bash
# Статус контейнеров
docker compose ps

# Логи в реальном времени
docker compose logs -f

# Проверка RareBooks Service
curl http://rare-books.ru/api/test/setup-status

# Проверка May Messenger
curl https://messenger.rare-books.ru/health
```

## 📊 Ключевые улучшения

| Сервис | Было (start_period) | Стало | Интервал проверки |
|--------|---------------------|-------|-------------------|
| backend | 120s | **180s** | 10s (было 30s) |
| frontend | 60s | **90s** | 10s (было 30s) |
| maymessenger_backend | 60s | **120s** | 10s (было 15s) |
| maymessenger_web_client | 5s | **30s** | 10s (было 15s) |
| proxy (nginx) | 30s | **60s** | 30s |

## 🎯 Retry логика

Nginx теперь автоматически делает retry при ошибках:
- **Setup API**: до 5 попыток
- **Основной API**: до 3 попыток
- **Frontend**: до 3 попыток
- **Messenger API**: до 3 попыток

## 💡 Важно

- Первый запуск может занять **3-4 минуты** - это нормально
- Все последующие запуски будут быстрее
- Не нужно делать `docker compose restart` после сборки
- При проблемах с подключением nginx автоматически сделает retry

## 🆘 Если не работает

1. Проверьте логи:
```bash
docker compose logs proxy
docker compose logs backend
```

2. Убедитесь что все контейнеры healthy:
```bash
docker compose ps
```

3. Подождите полной инициализации (3-4 минуты)

4. Если проблема сохраняется - смотрите детальное описание в `FIX_502_BAD_GATEWAY.md`

## ✨ Бонус

Теперь система более устойчива к временным сбоям:
- Автоматическое восстановление при проблемах с сетью
- Более быстрое обнаружение проблем
- Правильная работа keepalive соединений
- Оптимизированное DNS резолвинг

