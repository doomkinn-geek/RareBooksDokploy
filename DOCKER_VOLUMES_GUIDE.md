# Руководство по хранению данных в Docker

## Проблема

При пересоздании Docker контейнеров (например, после изменения кода):
- ✅ **База данных сохраняется** — данные хранятся в отдельных Docker volumes (`db_books_data`, `db_users_data`)
- ❌ **Изображения коллекций терялись** — хранились внутри контейнера backend в `wwwroot/collection_images/`

## Решение

Создан отдельный Docker volume `collection_images` для надежного хранения изображений пользовательских коллекций.

## Архитектура хранения данных

```
┌─────────────────────────────────────────────────────────┐
│                    Docker Host                          │
│                                                          │
│  ┌──────────────────┐  ┌──────────────────┐            │
│  │  db_books:       │  │  db_users:       │            │
│  │  PostgreSQL      │  │  PostgreSQL      │            │
│  └────────┬─────────┘  └────────┬─────────┘            │
│           │                     │                       │
│           │                     │                       │
│  ┌────────▼─────────────────────▼─────────┐            │
│  │     Docker Volumes (Persistent)        │            │
│  │                                         │            │
│  │  • db_books_data   (База книг)         │            │
│  │  • db_users_data   (База пользователей)│            │
│  │  • collection_images (Изображения)     │            │
│  └─────────────────────┬───────────────────┘            │
│                        │                                │
│  ┌─────────────────────▼───────────────────┐            │
│  │  backend: .NET Core WebAPI              │            │
│  │                                          │            │
│  │  Монтирование:                           │            │
│  │  collection_images ->                    │            │
│  │  /app/wwwroot/collection_images/         │            │
│  └──────────────────────────────────────────┘            │
└─────────────────────────────────────────────────────────┘
```

## Изменения в docker-compose.yml

### 1. Добавлен volume для backend:

```yaml
backend:
  # ... другие настройки ...
  volumes:
    # Постоянное хранилище для изображений коллекций пользователей
    - collection_images:/app/wwwroot/collection_images
```

### 2. Объявлен новый volume:

```yaml
volumes:
  db_books_data:
  db_users_data:
  collection_images:  # Постоянное хранилище для изображений коллекций пользователей
```

## Структура хранения изображений

```
/app/wwwroot/collection_images/
├── {userId1}/
│   ├── {bookId1}/
│   │   ├── image1.jpg
│   │   ├── image2.jpg
│   │   └── ...
│   ├── {bookId2}/
│   │   └── ...
│   └── ...
├── {userId2}/
│   └── ...
└── ...
```

## Применение изменений

### 1. Остановите текущие контейнеры:

```bash
docker-compose down
```

### 2. Пересоздайте контейнеры с новой конфигурацией:

```bash
docker-compose up -d --build
```

### 3. Проверьте, что volume создан:

```bash
docker volume ls | grep collection_images
```

Вы должны увидеть:

```
local     rarebooksdokploy_collection_images
```

### 4. Проверьте монтирование в контейнере:

```bash
docker exec rarebooks_backend ls -la /app/wwwroot/collection_images
```

## Управление данными

### Просмотр содержимого volume:

```bash
# Запустите временный контейнер для просмотра
docker run --rm -v rarebooksdokploy_collection_images:/data alpine ls -la /data
```

### Резервное копирование изображений:

```bash
# Создайте backup
docker run --rm \
  -v rarebooksdokploy_collection_images:/source:ro \
  -v $(pwd)/backups:/backup \
  alpine tar czf /backup/collection_images_$(date +%Y%m%d_%H%M%S).tar.gz -C /source .
```

### Восстановление из backup:

```bash
# Распакуйте backup
docker run --rm \
  -v rarebooksdokploy_collection_images:/target \
  -v $(pwd)/backups:/backup \
  alpine sh -c "cd /target && tar xzf /backup/collection_images_YYYYMMDD_HHMMSS.tar.gz"
```

### Очистка старых данных (ОСТОРОЖНО!):

```bash
# Удалите volume (только после остановки контейнеров!)
docker-compose down
docker volume rm rarebooksdokploy_collection_images
```

## Миграция существующих изображений

Если у вас уже есть изображения в старом контейнере:

### 1. Скопируйте изображения из работающего контейнера:

```bash
# Создайте временную директорию
mkdir -p ~/temp_collection_images

# Скопируйте из контейнера
docker cp rarebooks_backend:/app/wwwroot/collection_images/. ~/temp_collection_images/
```

### 2. Пересоздайте контейнеры с новой конфигурацией:

```bash
docker-compose down
docker-compose up -d --build
```

### 3. Скопируйте изображения в новый volume:

```bash
docker cp ~/temp_collection_images/. rarebooks_backend:/app/wwwroot/collection_images/
```

### 4. Установите правильные права доступа:

```bash
docker exec rarebooks_backend chown -R app:app /app/wwwroot/collection_images
docker exec rarebooks_backend chmod -R 755 /app/wwwroot/collection_images
```

### 5. Удалите временную директорию:

```bash
rm -rf ~/temp_collection_images
```

## Мониторинг использования дискового пространства

### Проверка размера volume:

```bash
docker system df -v | grep collection_images
```

### Проверка размера директории в контейнере:

```bash
docker exec rarebooks_backend du -sh /app/wwwroot/collection_images
```

### Детальная информация по пользователям:

```bash
docker exec rarebooks_backend du -sh /app/wwwroot/collection_images/*
```

## Преимущества нового подхода

✅ **Постоянство данных** — изображения сохраняются при пересоздании контейнеров
✅ **Независимость от кода** — изменения в backend не влияют на изображения
✅ **Простое резервное копирование** — можно делать backup только volume
✅ **Масштабируемость** — можно использовать внешние хранилища (NFS, AWS EFS и т.д.)
✅ **Изоляция** — данные изолированы от других контейнеров

## Дополнительные опции

### Использование именованного пути на хосте (bind mount):

Если хотите хранить изображения в определенной директории на хосте:

```yaml
backend:
  volumes:
    # Вместо volume используем bind mount
    - /path/on/host/collection_images:/app/wwwroot/collection_images
```

**Преимущества:**
- Прямой доступ к файлам с хоста
- Легкое резервное копирование через стандартные инструменты

**Недостатки:**
- Менее переносимо между системами
- Требует ручной настройки прав доступа

### Использование внешнего хранилища (NFS):

```yaml
volumes:
  collection_images:
    driver: local
    driver_opts:
      type: nfs
      o: addr=nfs-server.example.com,rw
      device: ":/path/to/collection_images"
```

## Проверка работоспособности

### 1. Загрузите изображение через API:

```bash
curl -X POST http://localhost/api/usercollection/{bookId}/images \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -F "file=@/path/to/image.jpg"
```

### 2. Проверьте наличие файла в volume:

```bash
docker exec rarebooks_backend find /app/wwwroot/collection_images -type f -name "*.jpg"
```

### 3. Пересоздайте контейнер backend:

```bash
docker-compose restart backend
```

### 4. Убедитесь, что изображение все еще доступно:

```bash
curl http://localhost/api/usercollection/{bookId}/images/{fileName} \
  -H "Authorization: Bearer YOUR_TOKEN" \
  --output test_image.jpg
```

Если файл скачался успешно — все работает правильно! 🎉

## Troubleshooting

### Проблема: "Permission denied" при записи изображений

**Решение:**

```bash
docker exec rarebooks_backend chown -R app:app /app/wwwroot/collection_images
docker exec rarebooks_backend chmod -R 755 /app/wwwroot/collection_images
```

### Проблема: Volume не монтируется

**Проверка:**

```bash
docker inspect rarebooks_backend | grep -A 10 Mounts
```

**Пересоздание:**

```bash
docker-compose down -v  # ОСТОРОЖНО: удалит все volumes!
docker-compose up -d
```

### Проблема: Недостаточно места на диске

**Проверка свободного места:**

```bash
docker system df
```

**Очистка неиспользуемых данных:**

```bash
docker system prune -a --volumes  # ОСТОРОЖНО: удалит неиспользуемые volumes!
```

## Дополнительная безопасность

### 1. Только чтение для определенных пользователей:

```yaml
backend:
  volumes:
    - collection_images:/app/wwwroot/collection_images:ro  # read-only
```

### 2. Шифрование volume (Linux):

Используйте LUKS или dm-crypt для шифрования volume на уровне хоста.

### 3. Регулярные backup:

Настройте cron job для автоматического резервного копирования:

```bash
# Добавьте в crontab:
0 2 * * * /path/to/backup_script.sh
```

## Заключение

Теперь изображения пользовательских коллекций хранятся надежно и не будут потеряны при обновлении кода сервера! 🚀

