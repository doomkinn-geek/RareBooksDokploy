# 📸 Изображения - Сжатие и локальное хранение

## Архитектура обработки изображений

```
┌─────────────────┐
│  Пользователь   │
│  выбирает фото  │
└────────┬────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│  ImagePickerButton                      │
│  - Сжатие до 70% качества              │
│  - Ограничение 1920x1920 px            │
│  - Проверка размера ≤ 10MB             │
└────────┬────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│  Локальное хранение                     │
│  1. Копия в device storage              │
│  2. SQLite: localImagePath              │
└────────┬────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│  Отправка на сервер                     │
│  POST /api/messages/image               │
│  FormData: imageFile                    │
└────────┬────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│  Сервер                                 │
│  - Валидация формата                    │
│  - Сохранение в /wwwroot/images/        │
│  - FilePath: /images/guid.jpg           │
└────────┬────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────────┐
│  Автоматическая очистка (7 дней)       │
│  - MediaCleanupService                  │
│  - Удаляет с сервера                    │
│  - Локальная копия остается!            │
└─────────────────────────────────────────┘
```

---

## Сжатие на клиенте

### ImagePickerButton - Параметры сжатия

**Файл:** `src/components/ImagePickerButton.tsx`

```typescript
const options: ImageLibraryOptions = {
  mediaType: 'photo',
  quality: 0.7,        // 70% качества (баланс)
  maxWidth: 1920,      // Full HD ширина
  maxHeight: 1920,     // Full HD высота
  includeBase64: false // Без base64 (экономия памяти)
};
```

### Процесс сжатия

1. **Выбор изображения** → react-native-image-picker
2. **Автоматическое изменение размера** → maxWidth/maxHeight
3. **Сжатие JPEG** → quality: 0.7
4. **Проверка размера** → максимум 10MB
5. **Результат** → оптимизированный файл готов к отправке

### Примеры сжатия

| Исходное фото | После сжатия | Экономия |
|---------------|--------------|----------|
| 4032x3024 (4.5MB) | 1920x1440 (850KB) | 81% |
| 3264x2448 (3.2MB) | 1920x1440 (720KB) | 78% |
| 1920x1080 (2.1MB) | 1920x1080 (650KB) | 69% |

---

## Локальное хранение на устройстве

### SQLite схема

**Таблица messages:**
```sql
CREATE TABLE messages (
  id TEXT PRIMARY KEY,
  chatId TEXT NOT NULL,
  type INTEGER NOT NULL,
  content TEXT,
  filePath TEXT,            -- Путь на сервере: /images/xxx.jpg
  localImagePath TEXT,      -- Локальный путь: file:///...
  localAudioPath TEXT,
  status INTEGER,
  createdAt TEXT,
  isLocalOnly INTEGER
);
```

### Процесс сохранения

1. **Выбор изображения** → получаем URI
   ```typescript
   asset.uri = "file:///data/user/0/com.depesha/cache/image.jpg"
   ```

2. **Копирование в постоянное хранилище**
   ```typescript
   import RNFS from 'react-native-fs';
   
   const permanentPath = `${RNFS.DocumentDirectoryPath}/images/${Date.now()}.jpg`;
   await RNFS.copyFile(asset.uri, permanentPath);
   ```

3. **Сохранение в SQLite**
   ```typescript
   await sqliteService.cacheMessage({
     id: message.id,
     chatId: chatId,
     type: MessageType.Image,
     filePath: '/images/server-guid.jpg',  // Сервер
     localImagePath: permanentPath,         // Локально
     content: null,
     status: MessageStatus.Sent
   });
   ```

4. **Отправка на сервер**
   ```typescript
   const formData = new FormData();
   formData.append('chatId', chatId);
   formData.append('imageFile', {
     uri: permanentPath,
     type: 'image/jpeg',
     name: 'image.jpg'
   });
   
   await api.post('/messages/image', formData);
   ```

### Отображение изображения

```typescript
import FastImage from 'react-native-fast-image';

const ImageMessage = ({ message }) => {
  // Приоритет: локальная копия → сервер
  const imageSource = message.localImagePath 
    ? { uri: message.localImagePath }
    : { uri: `${API_URL}${message.filePath}` };

  return (
    <FastImage
      source={imageSource}
      style={{ width: 200, height: 200 }}
      resizeMode={FastImage.resizeMode.cover}
    />
  );
};
```

---

## Хранение на сервере

### Структура директорий

```
/app/wwwroot/
├── audio/
│   └── guid.m4a
└── images/               ← Изображения
    ├── abc123.jpg
    ├── def456.png
    └── ...
```

### Автоматическая очистка

**MediaCleanupService** - запускается каждые 24 часа

**Алгоритм:**
1. Получить все изображения старше 7 дней
2. Удалить физические файлы из `/wwwroot/images/`
3. Обновить записи в БД:
   - `FilePath = null`
   - `Content = "[Изображение удалено с сервера]"`

**Важно:** Локальные копии на устройствах **НЕ УДАЛЯЮТСЯ**

### Конфигурация срока хранения

**appsettings.json:**
```json
{
  "MediaRetentionDays": 7
}
```

Изменить на 14 дней:
```json
{
  "MediaRetentionDays": 14
}
```

---

## Offline режим

### Сценарий 1: Отправка без интернета

1. Пользователь выбирает изображение
2. **Локально сохраняется:**
   - `localImagePath` = путь к файлу
   - `status` = Sending
   - `isLocalOnly` = true

3. Изображение **отображается в чате сразу**

4. При восстановлении соединения:
   - Фоновая синхронизация отправляет на сервер
   - Получает `filePath` от сервера
   - Обновляет запись в SQLite
   - `isLocalOnly` = false

### Сценарий 2: Просмотр старых сообщений offline

1. Сообщение было получено ранее → сохранено в SQLite
2. `localImagePath` существует
3. **Изображение загружается из локального хранилища**
4. Интернет не требуется!

---

## Преимущества архитектуры

### ✅ Для пользователя

- **Быстрая отправка** - сжатие на устройстве
- **Экономия трафика** - оптимизированные файлы
- **Offline доступ** - локальное хранение
- **История остается** - даже после удаления с сервера

### ✅ Для сервера

- **Экономия места** - автоматическая очистка
- **Меньше нагрузки** - сжатые файлы
- **Оптимизация БД** - старые записи обновляются

### ✅ Для разработки

- **Прозрачность** - понятная логика
- **Гибкость** - настраиваемые параметры
- **Масштабируемость** - легко добавить видео

---

## Настройка параметров

### Клиент (React Native)

**Изменить качество сжатия:**

`src/components/ImagePickerButton.tsx`
```typescript
quality: 0.7  // 0.1 (низкое) - 1.0 (максимальное)
```

**Изменить максимальный размер:**
```typescript
maxWidth: 1920   // Пиксели
maxHeight: 1920
```

**Изменить лимит размера файла:**
```typescript
if (asset.fileSize > 10 * 1024 * 1024) // 10MB
```

### Сервер (ASP.NET)

**Изменить лимит размера:**

`MessagesController.cs`
```csharp
if (imageFile.Length > 10 * 1024 * 1024) // 10MB
    return BadRequest("Image size must be less than 10MB");
```

**Изменить допустимые форматы:**
```csharp
var allowedExtensions = new[] { ".jpg", ".jpeg", ".png", ".gif", ".webp" };
```

**Изменить срок хранения:**

`appsettings.json`
```json
{
  "MediaRetentionDays": 7  // Дней
}
```

---

## Мониторинг и обслуживание

### Проверка размера папки изображений

**Linux:**
```bash
du -sh /app/wwwroot/images/
```

**Windows:**
```powershell
(Get-ChildItem "wwwroot\images" -Recurse | Measure-Object -Property Length -Sum).Sum / 1MB
```

### Логи очистки

```bash
# Linux
sudo journalctl -u maymessenger | grep "Media cleanup"

# Пример вывода:
# Media cleanup completed. Images: 142 files deleted, 142 records updated.
```

### Ручная очистка (если нужно)

```sql
-- Найти изображения старше 30 дней
SELECT * FROM "Messages" 
WHERE "Type" = 2 
  AND "CreatedAt" < NOW() - INTERVAL '30 days'
  AND "FilePath" IS NOT NULL;

-- Удалить физические файлы вручную
-- rm /app/wwwroot/images/old-guid.jpg

-- Обновить записи
UPDATE "Messages" 
SET "FilePath" = NULL, "Content" = '[Изображение удалено с сервера]'
WHERE "Type" = 2 
  AND "CreatedAt" < NOW() - INTERVAL '30 days';
```

---

## Рекомендации

### Production

1. **CDN:** Используйте CDN для раздачи изображений
2. **Backup:** Регулярно бэкапьте `/wwwroot/images/`
3. **Мониторинг:** Отслеживайте размер директории
4. **Оптимизация:** Настройте gzip/brotli компрессию в nginx

### Безопасность

1. **Антивирус:** Сканируйте загруженные файлы
2. **MIME проверка:** Проверяйте реальный тип файла
3. **Rate limiting:** Ограничьте частоту загрузок
4. **Права доступа:** Убедитесь что только владелец может удалить

---

## FAQ

**Q: Что происходит с локальной копией после удаления с сервера?**  
A: Локальная копия остается на устройстве пользователя навсегда (или до переустановки приложения).

**Q: Можно ли отключить автоматическую очистку?**  
A: Да, установите `"MediaRetentionDays": -1` в конфигурации (не рекомендуется).

**Q: Как пользователь может очистить локальное хранилище?**  
A: Через настройки приложения → Очистить кэш (функция должна быть реализована).

**Q: Поддерживаются ли GIF анимации?**  
A: Да, формат `.gif` разрешен на сервере.

**Q: Можно ли отправлять несколько изображений сразу?**  
A: В текущей реализации - по одному. Для пакетной отправки нужно расширить API.

---

**Дата:** 19 декабря 2025  
**Проект:** Депеша  
**Версия:** 1.0

