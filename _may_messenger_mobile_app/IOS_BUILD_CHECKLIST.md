# ✅ Чеклист для сборки iOS после изменений

## Статус подготовки iOS

### ✅ Что уже сделано:

1. **iOS конфигурация обновлена**
   - ✅ `Podfile` настроен
   - ✅ `Info.plist` содержит все разрешения
   - ✅ `Runner.entitlements` созданы для Push Notifications
   - ✅ `AppDelegate.swift` настроен для Firebase
   - ✅ CocoaPods зависимости установлены (51 pods)

2. **Flutter зависимости актуальны**
   - ✅ `flutter pub get` выполнен успешно
   - ✅ Все плагины совместимы с iOS

3. **Xcode проект обновлен**
   - ✅ `project.pbxproj` содержит правильные настройки
   - ✅ Development Team: `DM754J3JJS` настроен
   - ✅ Bundle ID: `com.maymessenger.mobileApp`

---

## ⚠️ Что нужно добавить для полноценной работы:

### 1. Firebase конфигурация (КРИТИЧНО)

#### Для iOS:
```bash
# Файл: ios/Runner/GoogleService-Info.plist
# Статус: ❌ ОТСУТСТВУЕТ
```

**Как получить:**
1. Откройте https://console.firebase.google.com
2. Выберите ваш проект
3. Добавьте iOS приложение (если еще не добавлено):
   - Bundle ID: `com.maymessenger.mobileApp`
4. Скачайте `GoogleService-Info.plist`
5. Скопируйте файл:
   ```bash
   cp ~/Downloads/GoogleService-Info.plist ios/Runner/
   ```
6. **ОБЯЗАТЕЛЬНО**: Добавьте в Xcode:
   ```bash
   open ios/Runner.xcworkspace
   ```
   - Правый клик на Runner → Add Files to "Runner"
   - Выберите `GoogleService-Info.plist`
   - ✅ Copy items if needed
   - ✅ Add to target: Runner

#### Для Android (для полноты):
```bash
# Файл: android/app/google-services.json
# Статус: ❌ ОТСУТСТВУЕТ
```

**Как получить:**
1. В том же Firebase проекте
2. Добавьте Android приложение:
   - Package name: `com.depesha`
3. Скачайте `google-services.json`
4. Скопируйте:
   ```bash
   cp ~/Downloads/google-services.json android/app/
   ```

---

## 🚀 Быстрая проверка сборки iOS

### Шаг 1: Проверка окружения
```bash
cd /Users/janaplett/RareBooksDokploy/_may_messenger_mobile_app

# Проверка Flutter
flutter doctor

# Должно быть:
# ✓ Flutter
# ✓ Xcode
```

### Шаг 2: Очистка и подготовка
```bash
# Очистка кеша
flutter clean

# Обновление зависимостей
flutter pub get

# Обновление iOS pods
cd ios
pod install --repo-update
cd ..
```

### Шаг 3: Тестовая сборка (без запуска)
```bash
# Сборка для симулятора
flutter build ios --simulator --debug

# Если успешно, попробуйте сборку для устройства
flutter build ios --debug --no-codesign
```

### Шаг 4: Запуск на симуляторе
```bash
# Список доступных симуляторов
flutter devices

# Запуск на iPhone симуляторе
flutter run -d "iPhone 15 Pro"
```

---

## 📋 Проверка совместимости iOS после изменений Android

### Проверьте следующие области:

#### 1. Разрешения (Permissions)
Если добавили новые разрешения в Android (`AndroidManifest.xml`), добавьте их в iOS (`Info.plist`):

**Текущие разрешения в Info.plist:**
- ✅ `NSMicrophoneUsageDescription` - микрофон
- ✅ `NSCameraUsageDescription` - камера
- ✅ `NSContactsUsageDescription` - контакты
- ✅ `NSPhotoLibraryUsageDescription` - галерея
- ✅ `NSPhotoLibraryAddUsageDescription` - сохранение фото
- ✅ `NSFaceIDUsageDescription` - Face ID
- ✅ `UIBackgroundModes` - фоновые режимы

**Если нужно добавить новые:**
Откройте `ios/Runner/Info.plist` и добавьте нужные ключи.

#### 2. Плагины и зависимости
Если добавили новые Flutter плагины в `pubspec.yaml`:

```bash
# Обновите зависимости
flutter pub get

# Обновите iOS pods
cd ios && pod install && cd ..
```

Убедитесь, что плагин поддерживает iOS (проверьте на pub.dev).

#### 3. Нативный код
Если изменяли Android нативный код (Kotlin/Java), проверьте нужны ли аналогичные изменения в iOS (Swift/Objective-C).

**Файлы для проверки:**
- `ios/Runner/AppDelegate.swift` - главный файл приложения
- `ios/Runner/Info.plist` - конфигурация
- Platform channels (если используются)

#### 4. Assets и ресурсы
Если добавили новые ассеты в `assets/`, они автоматически доступны на iOS через `pubspec.yaml`.

**Текущие assets:**
```yaml
assets:
  - assets/chat_background.png
```

#### 5. Иконки
Если обновили иконку приложения:

```bash
# Регенерируйте иконки
flutter pub run flutter_launcher_icons

# Проверьте
ls -la ios/Runner/Assets.xcassets/AppIcon.appiconset/
```

---

## 🔧 Решение типичных проблем

### Проблема 1: Firebase не инициализируется
```
Ошибка: [Firebase] Default FirebaseApp is not configured
```

**Решение:**
1. Убедитесь, что `GoogleService-Info.plist` добавлен в Xcode проект
2. Проверьте, что Bundle ID совпадает: `com.maymessenger.mobileApp`
3. Очистите и пересоберите:
   ```bash
   flutter clean
   cd ios && pod deintegrate && pod install && cd ..
   flutter run
   ```

### Проблема 2: Pod install не работает
```
Ошибка: Unable to find a specification for...
```

**Решение:**
```bash
cd ios
pod repo update
pod deintegrate
pod install --repo-update
cd ..
```

### Проблема 3: Signing ошибки
```
Ошибка: Signing for "Runner" requires a development team
```

**Решение:**
1. Откройте `ios/Runner.xcworkspace` в Xcode
2. Runner → Signing & Capabilities
3. Выберите ваш Team
4. Или настройте автоматический signing:
   - Automatic signing: ON
   - Team: Выберите из списка

### Проблема 4: Версия iOS слишком старая
```
Ошибка: Minimum deployment target is iOS 13.0
```

**Решение:**
Проект уже настроен на iOS 13.0+ в `Podfile` и `project.pbxproj`.
Если нужна более старая версия, измените:
```ruby
# В Podfile:
platform :ios, '12.0'  # или нужная версия
```

---

## 📝 Команды для быстрого старта

### Полная подготовка с нуля:
```bash
cd /Users/janaplett/RareBooksDokploy/_may_messenger_mobile_app

# 1. Очистка
flutter clean
rm -rf ios/Pods ios/Podfile.lock

# 2. Зависимости
flutter pub get

# 3. iOS pods
cd ios
pod install --repo-update
cd ..

# 4. Генерация иконок (если изменялись)
flutter pub run flutter_launcher_icons

# 5. Запуск
flutter run -d "iPhone 15 Pro"
```

### Быстрое обновление после изменений:
```bash
cd /Users/janaplett/RareBooksDokploy/_may_messenger_mobile_app

# Обновить зависимости
flutter pub get
cd ios && pod install && cd ..

# Запустить
flutter run
```

---

## ✅ Финальный чеклист перед релизом iOS

- [ ] `GoogleService-Info.plist` добавлен и в Xcode проекте
- [ ] APNs ключ загружен в Firebase Console
- [ ] Все разрешения добавлены в `Info.plist`
- [ ] Иконки сгенерированы и выглядят правильно
- [ ] Приложение запускается на симуляторе
- [ ] Приложение запускается на реальном устройстве
- [ ] Push уведомления работают (только на реальном устройстве)
- [ ] Тестирование всех функций выполнено
- [ ] Версия и build number обновлены в `pubspec.yaml`
- [ ] Signing настроен в Xcode
- [ ] Release сборка создается без ошибок

---

## 📚 Дополнительные ресурсы

- **Firebase iOS Setup**: См. `FIREBASE_CONFIG_GUIDE.md`
- **Полное руководство**: См. `IOS_DEPLOYMENT_GUIDE.md`
- **Установка инструментов**: См. `SETUP_FLUTTER_AND_FIREBASE.md`

---

## 🎯 Текущий статус

**Flutter окружение:**
```
✓ Flutter (3.38.6)
✓ Xcode (26.2)
✓ Проект настроен
```

**Что нужно добавить:**
1. ❌ `GoogleService-Info.plist` для iOS
2. ❌ `google-services.json` для Android (опционально)
3. ❌ APNs ключ в Firebase (для push на iOS)

**После добавления Firebase конфигурации приложение готово к сборке!** 🚀
