# 🔔 Настройка Push-уведомлений для iOS

## ⚠️ Критически важно!

Для работы push-уведомлений на iOS **обязательно** нужно настроить **APNs (Apple Push Notification service)** интеграцию между Firebase и Apple Developer Portal.

**Без APNs ключа уведомления НЕ будут работать на iOS!**

---

## 📋 Что нужно сделать (3 шага)

### ✅ Шаг 1: Создать APNs ключ в Apple Developer Portal

1. Откройте https://developer.apple.com/account/resources/authkeys/list

2. Нажмите **+** (Create a Key)

3. Заполните форму:
   - **Key Name:** `Depesha Push Notifications` (или любое другое имя)
   - ✅ Отметьте галочкой **Apple Push Notifications service (APNs)**

4. Нажмите **Continue** → **Register**

5. **ВАЖНО:** Скачайте файл `.p8` и сохраните:
   - **Key ID** (например: `AB12CD34EF`)
   - **Team ID** (находится справа вверху на странице, например: `DM754J3JJS`)
   
   ⚠️ **Файл .p8 можно скачать только один раз! Сохраните его в безопасном месте!**

6. Скачайте ключ, нажав **Download**

---

### ✅ Шаг 2: Загрузить APNs ключ в Firebase Console

1. Откройте Firebase Console: https://console.firebase.google.com

2. Выберите ваш проект

3. Перейдите в настройки:
   - Нажмите на **шестеренку** ⚙️ возле Project Overview
   - Выберите **Project settings**

4. Перейдите на вкладку **Cloud Messaging**

5. Прокрутите вниз до раздела **Apple app configuration**

6. В разделе **APNs authentication key** нажмите **Upload**

7. Загрузите файл `.p8` и введите:
   - **Key ID** (из шага 1)
   - **Team ID** (из шага 1, или найдите здесь: https://developer.apple.com/account)

8. Нажмите **Upload**

✅ **Готово! Теперь Firebase может отправлять уведомления на iOS!**

---

### ✅ Шаг 3: Проверить настройки в Xcode

1. Откройте проект в Xcode:
   ```bash
   cd /Users/janaplett/RareBooksDokploy/_may_messenger_mobile_app
   open ios/Runner.xcworkspace
   ```

2. Выберите **Runner** в Project Navigator (слева)

3. Перейдите на вкладку **Signing & Capabilities**

4. Убедитесь, что включены:
   - ✅ **Push Notifications**
   - ✅ **Background Modes** с отмеченными:
     - ✅ Remote notifications
     - ✅ Audio (для голосовых сообщений)
     - ✅ Background fetch

5. Проверьте, что:
   - **Team:** DM754J3JJS выбрана
   - **Provisioning Profile:** Automatic
   - **Signing Certificate:** Apple Development

---

## 📱 Шаг 4: Пересобрать и установить приложение

После настройки APNs нужно **пересобрать** приложение:

```bash
cd /Users/janaplett/RareBooksDokploy/_may_messenger_mobile_app

# Очистить кеш
flutter clean

# Переустановить зависимости
flutter pub get

# Пересобрать и установить на iPhone
flutter run --release
```

---

## 🧪 Тестирование уведомлений

### Способ 1: Через Firebase Console (самый простой)

1. Откройте Firebase Console: https://console.firebase.google.com

2. Перейдите в **Engage** → **Messaging** (в левом меню)

3. Нажмите **Create your first campaign** или **New campaign**

4. Выберите **Firebase Notification messages**

5. Заполните форму:
   - **Notification title:** `Тест`
   - **Notification text:** `Проверка push-уведомлений`

6. Нажмите **Next**

7. В разделе **Target** выберите:
   - **User segment:** All users
   - Или выберите конкретное приложение: **Депеша (iOS)**

8. Нажмите **Next** → **Review** → **Publish**

9. **Проверьте на iPhone** - уведомление должно прийти!

### Способ 2: Через тестирование в коде

После установки приложения проверьте в логах Xcode:

```bash
# Запустите приложение с логами
flutter run --release
```

Должны появиться сообщения:
```
APNs token received: [токен]
FCM token: [токен]
```

Если токены получены - всё работает правильно! ✅

### Способ 3: Через ваш backend

Отправьте тестовое уведомление с вашего сервера на полученный FCM токен.

---

## ❓ Решение проблем

### Проблема: "APNs token received", но "FCM token" не появляется

**Причина:** Firebase не может обменять APNs токен на FCM токен.

**Решение:**
1. Убедитесь, что APNs ключ загружен в Firebase Console
2. Проверьте, что Key ID и Team ID указаны правильно
3. Подождите 5-10 минут - первая синхронизация может занять время
4. Удалите и переустановите приложение:
   ```bash
   flutter clean
   flutter run --release
   ```

### Проблема: Нет "APNs token received" в логах

**Причина:** Приложение не запрашивает разрешение на уведомления или устройство не зарегистрировано.

**Решение:**
1. Проверьте на iPhone: **Настройки** → **Депеша** → **Уведомления** - должно быть разрешено
2. Если разрешение не запрашивалось:
   - Удалите приложение с iPhone
   - Переустановите: `flutter run --release`
   - При первом запуске разрешите уведомления

### Проблема: Уведомления не приходят, но токен получен

**Причина:** Возможно, проблема на backend или с форматом payload.

**Решение:**
1. Проверьте формат отправки уведомлений с backend
2. Для iOS обязательно включите `content_available: true` для фоновых уведомлений
3. Пример правильного payload для iOS:
   ```json
   {
     "message": {
       "token": "FCM_TOKEN",
       "notification": {
         "title": "Новое сообщение",
         "body": "Текст сообщения"
       },
       "apns": {
         "payload": {
           "aps": {
             "alert": {
               "title": "Новое сообщение",
               "body": "Текст сообщения"
             },
             "sound": "default",
             "badge": 1,
             "content-available": 1
           }
         }
       }
     }
   }
   ```

### Проблема: Уведомления приходят только когда приложение открыто

**Причина:** Не включен `content_available` или проблема с entitlements.

**Решение:**
1. Проверьте `Runner.entitlements`:
   ```xml
   <key>aps-environment</key>
   <string>production</string>
   ```
   Для Debug версии должен быть `development` в `RunnerDebug.entitlements`

2. Включите в payload: `"content-available": 1`

3. Убедитесь, что в Xcode включен **Background Modes** → **Remote notifications**

---

## 📊 Чеклист для отладки

- [ ] APNs ключ (.p8) создан в Apple Developer Portal
- [ ] Key ID и Team ID записаны
- [ ] APNs ключ загружен в Firebase Console (Cloud Messaging)
- [ ] GoogleService-Info.plist добавлен в Xcode проект
- [ ] Push Notifications включены в Signing & Capabilities
- [ ] Background Modes → Remote notifications включен
- [ ] Приложение пересобрано после настройки: `flutter clean && flutter run --release`
- [ ] На iPhone разрешены уведомления для приложения (Настройки → Депеша)
- [ ] В логах появляется "APNs token received"
- [ ] В логах появляется "FCM token"
- [ ] Тестовое уведомление через Firebase Console приходит

---

## 🎯 Краткая схема работы iOS уведомлений

```
1. Приложение регистрируется в APNs
        ↓
2. APNs возвращает device token
        ↓
3. Firebase обменивает APNs token на FCM token (нужен APNs ключ!)
        ↓
4. Backend отправляет уведомление на FCM
        ↓
5. FCM отправляет уведомление в APNs (используя ваш APNs ключ)
        ↓
6. APNs доставляет уведомление на iPhone
        ↓
7. ✅ Пользователь видит уведомление
```

**Без APNs ключа шаг 3 не работает, и уведомления не доставляются!**

---

## 📚 Дополнительная информация

### Полезные ссылки
- Apple Developer Portal (ключи): https://developer.apple.com/account/resources/authkeys/list
- Firebase Console: https://console.firebase.google.com
- APNs документация: https://developer.apple.com/documentation/usernotifications

### Файлы в проекте
- `ios/Runner/Runner.entitlements` - entitlements для Production
- `ios/Runner/RunnerDebug.entitlements` - entitlements для Debug/Development
- `ios/Runner/GoogleService-Info.plist` - Firebase конфигурация
- `ios/Runner/AppDelegate.swift` - инициализация Firebase и регистрация в APNs
- `ios/Runner/Info.plist` - настройки приложения, включая UIBackgroundModes

### Различия между Development и Production

**Development (тестирование):**
- `aps-environment: development` в entitlements
- Используется Development Provisioning Profile
- APNs sandbox сервер

**Production (App Store):**
- `aps-environment: production` в entitlements
- Используется App Store Distribution profile
- APNs production сервер

⚠️ **Один APNs ключ работает и для development, и для production!**

---

**Дата создания:** 13 января 2026  
**Bundle ID:** com.maymessenger.mobileApp  
**Team ID:** DM754J3JJS
