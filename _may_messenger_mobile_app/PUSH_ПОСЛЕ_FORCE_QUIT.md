# 📱 Push-уведомления после Force Quit на iOS

## ✅ Как ДОЛЖНО работать

На iOS push-уведомления **ДОЛЖНЫ приходить** в ЛЮБОМ состоянии приложения:
- ✅ Приложение открыто (foreground)
- ✅ Приложение свёрнуто (background)
- ✅ Приложение полностью закрыто (force quit / terminated)

**Это нормальное поведение iOS!**

---

## 🔍 Проверим, что происходит

### Тест 1: Уведомления когда приложение закрыто

1. **Полностью закройте приложение:**
   - Смахните вверх на iPhone (показать все приложения)
   - Смахните приложение "Депеша" вверх (force quit)

2. **Отправьте тестовое уведомление из Firebase Console:**
   ```
   https://console.firebase.google.com/project/maymessenger-3fdf0
   Engage → Messaging → New campaign
   Title: Тест после закрытия
   App: Депеша (iOS)
   Publish
   ```

3. **Проверьте iPhone:**
   - Уведомление ДОЛЖНО прийти как баннер
   - Даже если приложение полностью закрыто!

---

### Тест 2: Приложение запускается после force quit

1. **Полностью закройте приложение** (смахните из памяти)

2. **Откройте приложение снова** с домашнего экрана

3. **Что происходит?**
   - ✅ Приложение запускается нормально?
   - ❌ Приложение крашится или не запускается?

---

## ❌ Если приложение не запускается после force quit

### Возможные причины:

#### 1. Debug режим и hot reload

Если вы запускали через `flutter run` (debug mode), после force quit приложение может не запускаться из-за проблем с hot reload.

**Решение:** Установите в **Release режиме**:
```bash
cd /Users/janaplett/RareBooksDokploy/_may_messenger_mobile_app
flutter run --release
```

Или через Xcode:
- Product → Scheme → Edit Scheme
- Run → Build Configuration → **Release**

---

#### 2. Crash при запуске

Приложение может крашиться сразу при запуске.

**Проверка:** Запустите через Xcode с открытой консолью:
```bash
open ios/Runner.xcworkspace
```

1. Нажмите ▶️ Run
2. Откройте консоль (Cmd+Shift+Y)
3. Смахните приложение из памяти
4. Запустите приложение с домашнего экрана iPhone
5. **Смотрите логи в консоли Xcode** - там будет ошибка

---

#### 3. Проблема с сертификатом

Если приложение установлено через Development сертификат и срок доверия истёк.

**Решение:**
- Настройки iPhone → Основные → VPN и управление устройством
- Проверьте, что Developer доверен

---

## ✅ Про уведомления после force quit

### Важно понимать:

**iOS уведомления приходят ВСЕГДА**, даже когда:
- Приложение полностью закрыто (force quit)
- iPhone заблокирован
- Приложение не запускалось несколько дней

**Это работа самой iOS и APNs, не приложения!**

---

### Но есть одно условие:

Для **фоновых** уведомлений (когда приложение может обработать уведомление в фоне), backend должен отправлять правильный payload.

#### ❌ Неправильный payload (только показывает баннер):

```json
{
  "message": {
    "token": "FCM_TOKEN",
    "notification": {
      "title": "Тест",
      "body": "Сообщение"
    }
  }
}
```

#### ✅ Правильный payload (с фоновой обработкой):

```json
{
  "message": {
    "token": "FCM_TOKEN",
    "notification": {
      "title": "Новое сообщение",
      "body": "Текст сообщения"
    },
    "apns": {
      "headers": {
        "apns-priority": "10"
      },
      "payload": {
        "aps": {
          "alert": {
            "title": "Новое сообщение",
            "body": "Текст сообщения"
          },
          "sound": "default",
          "badge": 1,
          "content-available": 1,
          "mutable-content": 1
        }
      }
    },
    "data": {
      "chatId": "123",
      "messageId": "456",
      "type": "new_message"
    }
  }
}
```

**Ключевые параметры для iOS:**
- `"content-available": 1` - позволяет приложению проснуться в фоне и обработать уведомление
- `"mutable-content": 1` - позволяет Notification Service Extension модифицировать уведомление
- `"sound": "default"` - звук уведомления
- `"badge": 1` - бейдж на иконке
- `"apns-priority": "10"` - высокий приоритет доставки
- `"data"` - дополнительные данные для обработки

---

## 🧪 Тестирование

### Сценарий 1: Уведомление показывается всегда

1. Закройте приложение (force quit)
2. Отправьте уведомление из Firebase Console
3. **Результат:** Баннер появляется на экране iPhone ✅

**Это работает ВСЕГДА на iOS**, независимо от состояния приложения!

---

### Сценарий 2: Фоновая обработка уведомлений

Если вы хотите, чтобы приложение **обрабатывало** уведомления в фоне (обновляло счётчики, кешировало данные и т.д.), нужно:

1. ✅ **Backend отправляет** `"content-available": 1`
2. ✅ **Background Modes включены** в Xcode (уже включено)
3. ✅ **Реализован обработчик** в AppDelegate (уже реализован)

**Проверка:** Посмотрим, есть ли у нас обработчик фоновых уведомлений:

```swift
// В AppDelegate.swift должен быть:
override func application(
  _ application: UIApplication,
  didReceiveRemoteNotification userInfo: [AnyHashable: Any],
  fetchCompletionHandler completionHandler: @escaping (UIBackgroundFetchResult) -> Void
) {
  // Обработка фонового уведомления
}
```

---

## 🔧 Что проверить на backend

### 1. Формат отправки для iOS

Убедитесь, что backend использует **Firebase Cloud Messaging HTTP v1 API**:

```
POST https://fcm.googleapis.com/v1/projects/maymessenger-3fdf0/messages:send
```

С правильным payload (см. выше).

---

### 2. Проверка токена

FCM токен для вашего iPhone:
```
e2oDCwdJ-kLrt3mratI0dg:APA91bGNucjeOTBke8Fd0lWx8Xk0-Oc-A60y-sTOOY0xf1Lx9hO-_VeNlVDzoiBaekLxWlGj0RyJqR6Wy3bBidu_l6FmJYhAswGHNq9LnC-DcZOsTuLEgfk
```

Используйте этот токен для тестирования с backend.

---

### 3. Разница между Android и iOS payload

**Android** может работать только с:
```json
{
  "notification": {...},
  "data": {...}
}
```

**iOS** ТРЕБУЕТ секцию `apns` для правильной работы:
```json
{
  "notification": {...},
  "apns": {
    "payload": {
      "aps": {...}
    }
  },
  "data": {...}
}
```

Firebase автоматически конвертирует, но лучше отправлять правильный payload сразу.

---

## 📊 Таблица: Когда приходят уведомления

| Состояние приложения | Баннер показывается | Приложение обрабатывает |
|---------------------|---------------------|-------------------------|
| Открыто (foreground) | ✅ Да (если включено) | ✅ Да, мгновенно |
| Свёрнуто (background) | ✅ Да | ✅ Да (с `content-available`) |
| Закрыто (force quit) | ✅ Да | ⚠️ Только при открытии |
| iPhone заблокирован | ✅ Да | ⚠️ При разблокировке/открытии |

**Важно:**
- **Баннер** показывается iOS ВСЕГДА, независимо от состояния приложения
- **Обработка** уведомлений возможна только когда приложение:
  - Открыто
  - В фоне (background) - если есть `content-available: 1`
  - Закрыто - только когда пользователь откроет уведомление

---

## ✅ Что уже работает

Судя по логам, у вас:
- ✅ APNs токен получен
- ✅ FCM токен получен
- ✅ Firebase настроен правильно
- ✅ Разрешения на уведомления даны
- ✅ Background Modes настроены

**Уведомления ДОЛЖНЫ приходить даже после force quit!**

---

## 🧪 Проверьте прямо сейчас

1. **Полностью закройте приложение:**
   - Смахните из памяти (force quit)

2. **Отправьте тест из Firebase Console:**
   ```
   https://console.firebase.google.com/project/maymessenger-3fdf0
   Engage → Messaging → New campaign
   Title: Тест Force Quit
   Text: Проверка после закрытия
   App: Депеша (iOS)
   Publish
   ```

3. **Посмотрите на iPhone:**
   - Баннер ДОЛЖЕН появиться!
   - Даже если приложение закрыто!

4. **Если баннер НЕ появился:**
   - Проверьте: Настройки → Депеша → Уведомления → всё включено?
   - Проверьте: iPhone подключен к интернету?
   - Подождите 1-2 минуты (иногда есть задержка)

---

## 🆘 Если уведомления не приходят после force quit

### Проверьте на backend:

1. **Backend отправляет на правильный токен?**
   ```
   e2oDCwdJ-kLrt3mratI0dg:APA91bGNucjeOTBke8Fd0lWx8Xk0-Oc-A60y-sTOOY0xf1Lx9hO-_VeNlVDzoiBaekLxWlGj0RyJqR6Wy3bBidu_l6FmJYhAswGHNq9LnC-DcZOsTuLEgfk
   ```

2. **Backend отправляет с правильным payload?**
   - Должна быть секция `apns` для iOS
   - Должен быть `"content-available": 1`

3. **Backend получает ответ от Firebase?**
   - Проверьте логи backend
   - Firebase должен возвращать успешный ответ

---

## 📝 Итого

**Уведомления на iOS:**
- ✅ Работают ВСЕГДА, даже после force quit
- ✅ Это функция iOS, не приложения
- ✅ Ваше приложение правильно настроено

**Если уведомления не приходят после force quit:**
- Проверьте настройки уведомлений на iPhone
- Проверьте, что backend отправляет правильный payload с секцией `apns`
- Проверьте, что backend использует правильный FCM токен

**Если приложение не запускается после force quit:**
- Установите в Release режиме: `flutter run --release`
- Или проверьте логи в Xcode при запуске

---

**Запустите тесты прямо сейчас и дайте знать результат!** 🚀
