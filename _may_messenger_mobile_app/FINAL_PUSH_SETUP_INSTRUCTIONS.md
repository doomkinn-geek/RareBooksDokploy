# 🎯 Финальная настройка iOS Push-уведомлений

## ✅ Что вы уже сделали правильно

1. ✅ **Firebase Console:**
   - Проект: MayMessenger (maymessenger-3fdf0)
   - iOS приложение: Депеша (com.maymessenger.mobileApp)
   - GoogleService-Info.plist скопирован

2. ✅ **Apple Developer Portal:**
   - APNs ключ создан: "Depesha Push Notifications"
   - Key ID: 57HSVMW9B9
   - Team ID: DM754J3JJS

3. ✅ **Firebase Console - APNs ключи загружены:**
   - Development APNs auth key: 57HSVMW9B9 / DM754J3JJS
   - Production APNs auth key: 57HSVMW9B9 / DM754J3JJS

4. ✅ **Xcode:**
   - Bundle ID: com.maymessenger.mobileApp (совпадает с Firebase!)
   - Remote notifications включен в Signing & Capabilities

**Всё настроено идеально!** 🎉

---

## 🔧 Что нужно сделать сейчас

### Проблема
После добавления APNs ключа в Firebase Console нужно **ПЕРЕСОБРАТЬ** приложение, чтобы изменения вступили в силу.

### Решение (5 минут)

#### Автоматический способ:

```bash
cd /Users/janaplett/RareBooksDokploy/_may_messenger_mobile_app

# Запустите скрипт пересборки
chmod +x rebuild_and_test_push.sh
./rebuild_and_test_push.sh
```

Скрипт:
1. Очистит кеш (`flutter clean`)
2. Обновит зависимости (`flutter pub get`)
3. Переустановит CocoaPods (`pod install`)
4. Соберёт и установит приложение на iPhone (`flutter run --release`)

---

#### Ручной способ:

```bash
cd /Users/janaplett/RareBooksDokploy/_may_messenger_mobile_app

# 1. Очистка
flutter clean

# 2. Зависимости
flutter pub get

# 3. CocoaPods
cd ios
pod deintegrate
pod install
cd ..

# 4. Сборка и установка
flutter run --release
```

---

## 📋 Что проверить в логах

### ✅ Успех - если увидите ОБА сообщения:

```
APNs token received: <длинный_токен>
FCM registration token: <другой_длинный_токен>
```

**Это значит:**
- ✅ Приложение зарегистрировалось в APNs
- ✅ Firebase получил APNs токен и сконвертировал его в FCM токен
- ✅ Push-уведомления должны работать!

---

### ⚠️ Проблема - если только одно сообщение:

```
APNs token received: <токен>
```

**Но НЕТ:**
```
FCM registration token: <токен>
```

**Это значит:**
- ✅ Приложение работает с APNs
- ❌ Firebase не может сконвертировать APNs токен в FCM токен
- ❌ Возможно, проблема с APNs ключом в Firebase Console

**Решение:**
1. Проверьте в Firebase Console: Project settings → Cloud Messaging
2. Убедитесь, что ключ загружен для **Production** (не только Development)
3. Проверьте Key ID и Team ID
4. Попробуйте удалить и заново загрузить APNs ключ
5. Подождите 5-10 минут (первая синхронизация может занять время)
6. Переустановите приложение

---

### ❌ Нет ни одного токена

**Причины:**
1. Разрешение на уведомления не дано
2. iPhone не подключен к интернету
3. Проблема с GoogleService-Info.plist

**Решение:**
1. Удалите приложение с iPhone
2. Переустановите: `flutter run --release`
3. При первом запуске нажмите **"Разрешить"** на запрос уведомлений
4. Проверьте: **Настройки iPhone → Депеша → Уведомления** - должно быть включено

---

## 🧪 Тестирование после установки

### Способ 1: Firebase Console (рекомендуется)

```
🌐 Откройте: https://console.firebase.google.com

1. Выберите проект: MayMessenger
2. Engage → Messaging (в левом меню)
3. New campaign → Firebase Notification messages
4. Заполните:
   - Notification title: Тест
   - Notification text: Проверка iOS уведомлений
5. Next
6. Target:
   - App: Депеша (iOS) - выберите iOS приложение!
   - User segment: All users
7. Next → Review → Publish
```

**Проверьте iPhone - уведомление должно прийти!** 📱

---

### Способ 2: Через ваш backend API

После получения FCM токена отправьте тестовое уведомление с вашего backend сервера.

**Формат payload для iOS:**

```json
{
  "message": {
    "token": "FCM_TOKEN_ЗДЕСЬ",
    "notification": {
      "title": "Тест",
      "body": "Проверка уведомлений"
    },
    "apns": {
      "payload": {
        "aps": {
          "alert": {
            "title": "Тест",
            "body": "Проверка уведомлений"
          },
          "sound": "default",
          "badge": 1,
          "content-available": 1
        }
      }
    }
  }
}
```

**⚠️ ВАЖНО для iOS:**
- `"content-available": 1` - обязательно для фоновых уведомлений
- `"sound": "default"` - чтобы был звук
- `"badge": 1` - чтобы был бейдж на иконке

---

## 📊 Контрольный чеклист

### Перед пересборкой
- [x] APNs ключ создан в Apple Developer Portal
- [x] APNs ключ загружен в Firebase Console (Production)
- [x] GoogleService-Info.plist скопирован в ios/Runner/
- [x] Bundle ID совпадает (com.maymessenger.mobileApp)
- [x] Remote notifications включен в Xcode

### После пересборки
- [ ] Приложение установлено на iPhone
- [ ] Разрешение на уведомления дано
- [ ] В логах есть "APNs token received"
- [ ] В логах есть "FCM registration token"
- [ ] Тестовое уведомление из Firebase Console приходит

---

## ❓ Частые проблемы

### 1. Уведомления работают только когда приложение открыто

**Причина:** Backend не отправляет `"content-available": 1`

**Решение:** Добавьте в payload:
```json
"apns": {
  "payload": {
    "aps": {
      "content-available": 1
    }
  }
}
```

---

### 2. В логах только APNs токен, но нет FCM токена

**Причина:** Firebase не может обменять APNs токен на FCM токен (проблема с APNs ключом).

**Решение:**
1. Проверьте Firebase Console → Cloud Messaging → Production APNs auth key
2. Убедитесь, что Key ID и Team ID правильные
3. Попробуйте удалить и заново загрузить ключ
4. Подождите 10 минут
5. Переустановите приложение

---

### 3. Нет ни одного токена в логах

**Причина:** Разрешение на уведомления не дано.

**Решение:**
1. Проверьте на iPhone: **Настройки → Депеша → Уведомления**
2. Удалите приложение
3. Переустановите: `flutter run --release`
4. При первом запуске нажмите **"Разрешить"**

---

### 4. "Нет подключённых устройств"

**Причина:** iPhone не подключен или не разрешён для разработки.

**Решение:**
1. Подключите iPhone к Mac через USB
2. Разблокируйте iPhone
3. Если появится "Trust This Computer?" - нажмите "Trust"
4. Проверьте: `flutter devices`
5. Если не видит - откройте Xcode → Window → Devices and Simulators
6. Убедитесь, что устройство отображается

---

## 🔍 Проверка текущего состояния

Запустите скрипт проверки:

```bash
cd /Users/janaplett/RareBooksDokploy/_may_messenger_mobile_app
./check_ios_push_setup.sh
```

Он проверит все файлы конфигурации и покажет, что нужно исправить.

---

## 📞 Ссылки

- **Firebase Console (ваш проект):** https://console.firebase.google.com/project/maymessenger-3fdf0
- **Apple Developer (ключи):** https://developer.apple.com/account/resources/authkeys/list
- **APNs документация:** https://developer.apple.com/documentation/usernotifications

---

## 🚀 Следующие шаги

```bash
# 1. Подключите iPhone к Mac через USB
# 2. Разблокируйте iPhone
# 3. Запустите пересборку:

cd /Users/janaplett/RareBooksDokploy/_may_messenger_mobile_app
./rebuild_and_test_push.sh

# 4. При первом запуске РАЗРЕШИТЕ уведомления
# 5. Проверьте логи - должны быть оба токена
# 6. Отправьте тестовое уведомление из Firebase Console
```

---

## 🎯 Ожидаемый результат

После выполнения всех шагов:

✅ Приложение установлено на iPhone  
✅ В логах есть оба токена (APNs + FCM)  
✅ Тестовое уведомление из Firebase Console приходит  
✅ Уведомления приходят даже когда приложение закрыто  
✅ Push-уведомления полностью работают! 🎉

---

**Время выполнения:** 5-10 минут  
**Сложность:** Низкая (просто пересборка приложения)

**Начните прямо сейчас:** `./rebuild_and_test_push.sh` 🚀
