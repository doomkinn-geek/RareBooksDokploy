# Быстрый старт отладки

## ✅ Проверка готовности

Ваше устройство уже подключено и готово к отладке:
- **Устройство**: SM A556E (RZCX91YSZSH)
- **Android**: 16 (API 36)
- **Flutter**: настроен и готов

## 🚀 Запуск отладки (3 шага)

### Шаг 1: Откройте проект в VS Code

```bash
code c:\rarebooks\rarebooks_mobile
```

### Шаг 2: Нажмите F5

Или выберите в меню: **Run > Start Debugging**

### Шаг 3: Выберите конфигурацию

Выберите **"RareBooks Mobile (Debug)"** из списка

## 📱 Что произойдет

1. Приложение скомпилируется
2. Установится на ваше устройство (SM A556E)
3. Запустится в режиме отладки
4. Появится возможность ставить breakpoints и отлаживать код

## 🔧 Полезные команды во время отладки

- **r** - Hot Reload (быстрая перезагрузка)
- **R** - Hot Restart (полная перезагрузка)
- **q** - Выход

## 🐛 Установка breakpoints

1. Откройте любой `.dart` файл в `lib/`
2. Кликните слева от номера строки (появится красная точка)
3. При выполнении кода отладчик остановится на этой строке

## 📋 Проверка подключения устройства

Если нужно проверить подключение:

```powershell
cd c:\rarebooks\rarebooks_mobile
.\scripts\check_device.ps1
```

## 🔍 Просмотр логов

Логи отображаются в панели **Debug Console** в VS Code.

Для просмотра логов Android:

```bash
adb logcat | grep flutter
```

## 📚 Подробная документация

См. [README_DEBUG.md](README_DEBUG.md) для полной документации.

