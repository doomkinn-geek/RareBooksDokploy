# Исправление ошибки авторизации - Сводка изменений

## 🐛 Проблема
При попытке авторизации пользователь получал ошибку "Ошибка соединения. Проверьте интернет-подключение", хотя сервер API был доступен.

## 🔍 Причины проблемы

### 1. Отсутствие разрешений Android
**Проблема**: В `AndroidManifest.xml` отсутствовали необходимые разрешения для доступа к интернету.

**Решение**: Добавлены разрешения:
```xml
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
```

### 2. Несоответствие форматов данных API
**Проблема**: Backend API возвращает JSON с полями, начинающимися с заглавной буквы (`Token`, `User`, `Email`, `UserName`, `HasSubscription`, `Role`), а Flutter модели ожидали поля с маленькой буквы.

**Ответ API**:
```json
{
  "Token": "eyJhbGc...",
  "User": {
    "Email": "user@example.com",
    "UserName": "User Name",
    "HasSubscription": false,
    "Role": "User"
  }
}
```

**Решение**: Добавлены аннотации `@JsonKey` для правильного маппинга полей:
```dart
@JsonSerializable()
class LoginResponse {
  @JsonKey(name: 'Token')
  final String token;
  
  @JsonKey(name: 'User')
  final User? user;
}

@JsonSerializable()
class User {
  @JsonKey(name: 'Id')
  final String id;
  
  @JsonKey(name: 'Email')
  final String? email;
  
  @JsonKey(name: 'UserName')
  final String? userName;
  
  @JsonKey(name: 'Role')
  final String? role;
  
  @JsonKey(name: 'HasSubscription')
  final bool hasSubscription;
}
```

### 3. Неправильный тип ID пользователя
**Проблема**: Backend использует `string` для ID пользователя (наследуется от `IdentityUser`), а Flutter модель ожидала `int`.

**Решение**: 
- Изменен тип поля `id` с `int` на `String`
- Добавлен геттер `numericId` для обратной совместимости:
```dart
int get numericId {
  try {
    return int.tryParse(id) ?? 0;
  } catch (e) {
    return 0;
  }
}
```

### 4. Недостаточная обработка ошибок
**Проблема**: Обработка ошибок Dio была недостаточно детальной для диагностики проблем подключения.

**Решение**: Улучшена обработка ошибок в `ApiService.login()`:
```dart
try {
  final response = await _dio.post(
    ApiConfig.authLogin,
    data: request.toJson(),
  );
  
  if (response.statusCode == 200) {
    return LoginResponse.fromJson(response.data);
  } else if (response.statusCode == 401) {
    throw Exception('Неверный email или пароль');
  } else if (response.statusCode == 400) {
    throw Exception('Некорректные данные для входа');
  } else {
    throw Exception('Ошибка сервера: ${response.statusCode}');
  }
} on DioException catch (e) {
  if (e.type == DioExceptionType.connectionTimeout ||
      e.type == DioExceptionType.receiveTimeout ||
      e.type == DioExceptionType.sendTimeout) {
    throw Exception('Превышено время ожидания. Проверьте подключение к интернету');
  } else if (e.type == DioExceptionType.connectionError) {
    throw Exception('Не удалось подключиться к серверу. Проверьте подключение к интернету');
  }
  // ... другие типы ошибок
}
```

## 📝 Измененные файлы

1. **android/app/src/main/AndroidManifest.xml**
   - ✅ Добавлены разрешения `INTERNET` и `ACCESS_NETWORK_STATE`
   - ✅ Обновлено название приложения на "Редкие Книги"
   - ✅ Добавлен `android:usesCleartextTraffic="false"` для безопасности

2. **lib/models/user.dart**
   - ✅ Изменен тип `id` с `int` на `String`
   - ✅ Добавлены аннотации `@JsonKey` для всех полей API
   - ✅ Переименовано поле `name` в `userName` с маппингом на `UserName`
   - ✅ Добавлен геттер `name` для получения отображаемого имени
   - ✅ Добавлен геттер `numericId` для обратной совместимости

3. **lib/services/api_service.dart**
   - ✅ Добавлено логирование всех API запросов и ответов
   - ✅ Улучшена обработка ошибок в методе `login()`
   - ✅ Добавлена детальная обработка различных типов `DioException`
   - ✅ Добавлена опция `validateStatus` для ручной обработки статус-кодов

4. **lib/services/auth_service.dart**
   - ✅ Обновлены вызовы `saveUserId()` для использования `numericId` вместо `id`

## 🧪 Тестирование

### Как протестировать исправления:

1. **Установите обновленное приложение**:
   ```bash
   flutter install
   # или
   adb install -r build/app/outputs/flutter-apk/app-debug.apk
   ```

2. **Тест авторизации**:
   - Откройте приложение
   - Нажмите "Войти"
   - Введите корректные учетные данные: `doomkinn@yandex.ru`
   - При успешной авторизации вы будете перенаправлены на главный экран
   - При неверных учетных данных увидите сообщение "Неверный email или пароль"
   - При отсутствии интернета увидите "Не удалось подключиться к серверу"

3. **Проверка логов**:
   ```bash
   flutter logs
   # или
   adb logcat | grep -i "ApiService\|flutter"
   ```
   
   В логах должны появиться сообщения:
   ```
   [ApiService] API Request: POST https://rare-books.ru/api/auth/login
   [ApiService] API Response: 200 /auth/login
   ```

## 🔐 Безопасность

- ✅ `android:usesCleartextTraffic="false"` - запрещен HTTP трафик, разрешен только HTTPS
- ✅ API использует HTTPS с валидным SSL сертификатом
- ✅ JWT токены хранятся в `flutter_secure_storage`
- ✅ Пароли никогда не сохраняются локально

## 🚀 Результат

Теперь приложение:
- ✅ Корректно подключается к API
- ✅ Правильно обрабатывает ответы от сервера
- ✅ Показывает понятные сообщения об ошибках
- ✅ Логирует все API запросы для диагностики
- ✅ Соответствует требованиям безопасности Android

## 📊 Проверка конфигурации Docker и Nginx

Конфигурация сервера проверена и работает корректно:

### Docker Compose
- ✅ Backend доступен на `rarebooks_backend:80`
- ✅ Proxy (Nginx) проксирует API на порт 443 (HTTPS)
- ✅ SSL сертификаты настроены правильно

### Nginx
- ✅ HTTPS настроен на домене `rare-books.ru`
- ✅ SSL сертификаты от Let's Encrypt установлены
- ✅ Маршрут `/api/auth/login` корректно проксируется на backend
- ✅ HTTP -> HTTPS redirect работает (кроме специальных endpoint'ов)

## 🔄 Следующие шаги

Рекомендации для дальнейшей разработки:

1. **Добавить unit-тесты** для `AuthService` и `ApiService`
2. **Реализовать refresh token** для автоматического обновления токена
3. **Добавить биометрическую аутентификацию** (отпечаток пальца)
4. **Кэширование данных** для работы офлайн

## 📞 Поддержка

При возникновении проблем проверьте:
1. Доступность сервера: `https://rare-books.ru/api/test/health`
2. Логи приложения: `flutter logs`
3. Логи сервера: `docker logs rarebooks_backend`

