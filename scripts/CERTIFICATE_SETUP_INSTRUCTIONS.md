# Инструкция по настройке SSL сертификатов для messenger.rare-books.ru

## Предварительные требования

1. ✓ Docker и Docker Compose установлены
2. ✓ Домен `messenger.rare-books.ru` настроен в DNS (A-запись указывает на IP сервера)
3. ✓ Nginx запущен и отвечает на порту 80/443
4. ✓ Certbot установлен на хосте: `sudo apt install certbot -y`

---

## Шаг 1: Проверка DNS настроек

Перед получением сертификата убедитесь, что DNS запись настроена правильно:

```bash
# Проверка DNS записи
nslookup messenger.rare-books.ru

# Или с помощью dig
dig messenger.rare-books.ru
```

Должен возвращаться IP адрес вашего сервера.

---

## Шаг 2: Подготовка папки для challenge-файлов

Создайте папку для Certbot challenge-файлов (если еще не создана):

```bash
sudo mkdir -p /var/www/certbot
```

Эта папка уже должна быть смонтирована в Docker контейнер nginx через `docker-compose.yml`.

---

## Шаг 3: Проверка работы nginx

Убедитесь, что nginx отвечает и может обработать challenge запросы:

```bash
# Проверка доступности домена
curl -I http://messenger.rare-books.ru

# Должен вернуться редирект 301 или 200 OK
```

---

## Шаг 4: Получение SSL сертификата

Запустите скрипт для получения сертификата:

```bash
# Перейдите в папку проекта
cd /home/docker/RareBooksDokploy
# или
cd /home/youruser/RareBooksDokploy

# Сделайте скрипт исполняемым
chmod +x scripts/get_messenger_certificate.sh

# Запустите скрипт
sudo scripts/get_messenger_certificate.sh
```

**Или выполните вручную:**

```bash
sudo certbot certonly --webroot \
  -w /var/www/certbot \
  -d messenger.rare-books.ru
```

### Что делать, если возникла ошибка?

**Ошибка: "Connection refused"**
- Проверьте, что nginx запущен: `docker ps | grep nginx`
- Проверьте порты: `sudo netstat -tulpn | grep -E ':(80|443)'`

**Ошибка: "DNS problem: NXDOMAIN"**
- DNS запись еще не распространилась. Подождите 5-30 минут
- Проверьте DNS: `nslookup messenger.rare-books.ru`

**Ошибка: "Failed authorization procedure"**
- Убедитесь, что nginx может обработать `/.well-known/acme-challenge/`
- Проверьте логи nginx: `docker logs nginx_container`

---

## Шаг 5: Проверка полученного сертификата

После успешного получения сертификата проверьте файлы:

```bash
sudo ls -la /etc/letsencrypt/live/messenger.rare-books.ru/

# Должны быть файлы:
# - fullchain.pem
# - privkey.pem
# - cert.pem
# - chain.pem
```

---

## Шаг 6: Перезапуск nginx для применения нового сертификата

```bash
# Перейдите в папку проекта
cd /home/docker/RareBooksDokploy
# или
cd /home/youruser/RareBooksDokploy

# Перезапустите nginx контейнер
docker-compose restart proxy

# Проверьте логи
docker logs nginx_container
```

---

## Шаг 7: Проверка работы HTTPS

Проверьте, что сертификат работает правильно:

```bash
# Проверка сертификата
curl -I https://messenger.rare-books.ru/api

# Проверка через браузер
# Откройте: https://messenger.rare-books.ru/swagger
```

**Проверка сертификата с помощью OpenSSL:**

```bash
openssl s_client -connect messenger.rare-books.ru:443 -servername messenger.rare-books.ru </dev/null 2>/dev/null | openssl x509 -noout -text | grep -A2 "Subject:"

# Должно показать:
# Subject: CN = messenger.rare-books.ru
```

---

## Шаг 8: Настройка автоматического обновления сертификатов

### 8.1. Обновите путь в скрипте обновления

Отредактируйте `scripts/renew_all_certificates.sh` и укажите правильный путь к вашему проекту:

```bash
nano scripts/renew_all_certificates.sh

# Найдите строку:
cd /home/docker/RareBooksDokploy || cd /home/youruser/RareBooksDokploy

# Измените на ваш реальный путь, например:
cd /home/doomkinn/RareBooksDokploy
```

### 8.2. Сделайте скрипт исполняемым

```bash
sudo cp scripts/renew_all_certificates.sh /usr/local/bin/renew_all_certificates.sh
sudo chmod +x /usr/local/bin/renew_all_certificates.sh
```

### 8.3. Добавьте задачу в cron

```bash
# Откройте crontab
sudo crontab -e

# Добавьте строку (выполнять каждый день в 3:00):
0 3 * * * /usr/local/bin/renew_all_certificates.sh >> /var/log/renew_cert.log 2>&1
```

Сохраните и закройте файл.

### 8.4. Проверка работы автообновления

Протестируйте скрипт вручную:

```bash
# Тестовый запуск
sudo /usr/local/bin/renew_all_certificates.sh

# Проверьте лог
cat /var/log/renew_cert.log
```

---

## Шаг 9: Финальная проверка

### Проверка через curl

```bash
# Health check
curl https://messenger.rare-books.ru/health

# API endpoint
curl https://messenger.rare-books.ru/api/auth/test
```

### Проверка через браузер

Откройте в браузере:
- https://messenger.rare-books.ru/swagger - должен открыться Swagger UI
- https://messenger.rare-books.ru/api - API должен отвечать

**Проверьте сертификат:**
- Нажмите на замочек в адресной строке браузера
- Посмотрите информацию о сертификате
- Должно быть: "Выдан для: messenger.rare-books.ru"
- Срок действия: ~90 дней

---

## Troubleshooting (Устранение проблем)

### Проблема: "NET::ERR_CERT_COMMON_NAME_INVALID"

**Причина:** Используется сертификат от другого домена

**Решение:**
1. Проверьте nginx конфигурацию:
   ```bash
   docker exec nginx_container cat /etc/nginx/nginx.conf | grep -A5 "server_name messenger.rare-books.ru"
   ```
2. Убедитесь, что используются правильные пути к сертификатам:
   ```
   ssl_certificate     /etc/letsencrypt/live/messenger.rare-books.ru/fullchain.pem;
   ssl_certificate_key /etc/letsencrypt/live/messenger.rare-books.ru/privkey.pem;
   ```
3. Перезапустите nginx: `docker-compose restart proxy`

### Проблема: Сертификат не найден в контейнере

**Причина:** Сертификат не смонтирован в контейнер

**Решение:**
1. Проверьте монтирование в `docker-compose.yml`:
   ```yaml
   volumes:
     - /etc/letsencrypt:/etc/letsencrypt:ro
   ```
2. Проверьте наличие сертификата на хосте:
   ```bash
   sudo ls -la /etc/letsencrypt/live/messenger.rare-books.ru/
   ```
3. Перезапустите контейнер: `docker-compose restart proxy`

### Проблема: CORS ошибки

**Причина:** Неправильная конфигурация прокси

**Решение:**
1. Проверьте логи nginx: `docker logs nginx_container`
2. Убедитесь, что заголовки прокси настроены правильно
3. Проверьте backend логи: `docker logs maymessenger_backend`

### Проблема: 502 Bad Gateway

**Причина:** Backend не отвечает

**Решение:**
1. Проверьте статус backend: `docker ps | grep maymessenger`
2. Проверьте логи: `docker logs maymessenger_backend`
3. Проверьте health endpoint: `curl http://localhost:5000/health` (внутри сервера)

---

## Полезные команды

```bash
# Просмотр всех сертификатов
sudo certbot certificates

# Ручное обновление всех сертификатов (dry-run)
sudo certbot renew --dry-run

# Ручное обновление всех сертификатов
sudo certbot renew

# Удаление сертификата (если нужно переполучить)
sudo certbot delete --cert-name messenger.rare-books.ru

# Проверка логов nginx
docker logs nginx_container --tail 100 -f

# Проверка логов backend
docker logs maymessenger_backend --tail 100 -f

# Перезапуск всех сервисов
docker-compose restart

# Проверка конфигурации nginx (внутри контейнера)
docker exec nginx_container nginx -t
```

---

## Структура сертификатов

После выполнения всех шагов у вас будут следующие сертификаты:

```
/etc/letsencrypt/live/
├── rare-books.ru/
│   ├── fullchain.pem     (для rare-books.ru)
│   └── privkey.pem
└── messenger.rare-books.ru/
    ├── fullchain.pem     (для messenger.rare-books.ru)
    └── privkey.pem
```

Каждый домен использует свой собственный сертификат!

---

## Срок действия сертификатов

- Let's Encrypt сертификаты действуют **90 дней**
- Certbot автоматически обновляет их за **30 дней до истечения**
- Cron задача проверяет обновления **каждый день в 3:00**
- Проверяйте логи в `/var/log/renew_cert.log`

---

## Контакты и поддержка

При возникновении проблем:
1. Проверьте логи: `/var/log/renew_cert.log`
2. Проверьте Docker логи: `docker logs nginx_container`
3. Проверьте статус сертификатов: `sudo certbot certificates`

**Готово! 🎉**

Теперь https://messenger.rare-books.ru/api должен работать с валидным SSL сертификатом!

