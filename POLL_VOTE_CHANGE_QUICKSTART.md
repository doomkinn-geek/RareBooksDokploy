# Изменение голоса в голосованиях - Быстрый старт

## Что изменилось?

Теперь пользователи могут **изменять свой голос** в групповых чатах до тех пор, пока создатель голосования не завершит его.

## Для пользователей

### Одиночный выбор (Radio)

1. ✅ Нажмите на любую опцию → голос зафиксирован
2. 🔄 Нажмите на другую опцию → голос автоматически изменится
3. 🔒 После завершения голосования изменения невозможны

### Множественный выбор (Checkbox)

1. ✅ Выберите несколько опций → нажмите "Голосовать"
2. 🔄 Измените галочки → нажмите "Изменить голос"
3. 💡 Подсказка: "Можно изменить выбор до завершения голосования"
4. 🔒 После завершения голосования изменения невозможны

### Визуальные индикаторы

**Голосование активно:**
- ☑️ Чекбоксы/радио-кнопки видны и активны
- 🔢 Показывается количество голосов
- 🔄 Можно изменить выбор

**Голосование завершено:**
- 📊 Прогресс-бары с процентами
- 🏁 Метка "Завершено"
- 🔒 Изменения заблокированы

## Изменённые файлы

### Frontend (Flutter)
- `_may_messenger_mobile_app/lib/presentation/widgets/poll_widget.dart` ✅

### Backend (C#)
- ❌ Изменений не требуется (уже поддерживает изменение голоса)

## Быстрое тестирование

```bash
# 1. Пересобрать Flutter приложение
cd _may_messenger_mobile_app
flutter clean
flutter pub get
flutter build apk --release
# или для iOS:
# flutter build ios --release

# 2. Установить на устройство и протестировать:
# - Создать голосование
# - Проголосовать
# - Изменить голос (должно работать)
# - Завершить голосование
# - Попытаться изменить голос (должно быть заблокировано)
```

## Технические детали

### Ключевые изменения в poll_widget.dart:

```dart
// 1. Результаты показываются только после завершения
final showResults = isClosed; // было: hasVoted || isClosed

// 2. Проверка изменений выбора
bool _hasChanges() {
  final currentVotes = widget.poll.myVotes.toSet();
  return _selectedOptions.length != currentVotes.length ||
         !_selectedOptions.every((id) => currentVotes.contains(id));
}

// 3. Кнопка с динамическим текстом
Text(hasVoted ? 'Изменить голос' : 'Голосовать')

// 4. Подсказка для пользователей
if (!isClosed && hasVoted && poll.allowMultipleAnswers)
  Text('Можно изменить выбор до завершения голосования')
```

## Real-time обновления

Изменения голосов отправляются через **SignalR** всем участникам чата в реальном времени:

```csharp
// Backend автоматически отправляет обновления
await _hubContext.Clients.Group(chatId)
    .SendAsync("PollUpdated", new { 
        MessageId = messageId, 
        Poll = updatedPollDto 
    });
```

## Безопасность

- ✅ Проверка членства в чате
- ✅ Проверка статуса голосования (открыто/закрыто)
- ✅ Проверка срока действия
- ✅ Корректное обновление счетчиков
- ✅ Предотвращение дублирования голосов

## FAQ

**Q: Можно ли изменить голос после завершения голосования?**  
A: Нет, после завершения голосования изменения невозможны.

**Q: Видят ли другие участники мои изменения голоса?**  
A: Да, если голосование не анонимное. В анонимных голосованиях показывается только общее количество голосов.

**Q: Влияет ли изменение голоса на счетчик "Всего проголосовало"?**  
A: Нет, счетчик показывает количество уникальных пользователей, проголосовавших хотя бы раз.

**Q: Нужно ли обновлять базу данных или бэкенд?**  
A: Нет, бэкенд уже поддерживает эту функциональность. Нужно только пересобрать Flutter приложение.

## Развёртывание

### Для мобильного приложения:

```bash
# Android
cd _may_messenger_mobile_app
flutter build apk --release
# Файл: build/app/outputs/flutter-apk/app-release.apk

# iOS (требуется macOS)
flutter build ios --release
# Через Xcode: Product → Archive → Distribute App
```

### Для бэкенда:
❌ Изменений не требуется, можно продолжать использовать текущую версию

## Поддержка

Если возникли проблемы:

1. Проверьте версию Flutter: `flutter --version` (рекомендуется 3.16+)
2. Очистите кэш: `flutter clean && flutter pub get`
3. Проверьте логи: поиск по `[POLL]` в консоли
4. Проверьте подключение к SignalR

---

**Дата:** 13 января 2026  
**Статус:** ✅ Готово к развёртыванию
