# ✅ УДАЛЕНИЕ ВСЕХ КНИГ + УЛУЧШЕННАЯ СОРТИРОВКА

Дата: 30 ноября 2025

---

## 🎯 Реализовано

1. **API endpoint для удаления всех книг** - `DELETE /api/usercollection/all`
2. **Кнопка "Удалить всё"** в интерфейсе коллекции
3. **Диалог подтверждения** с предупреждением
4. **Сортировка по дате покупки по умолчанию**
5. **Выбор направления сортировки** (по возрастанию/убыванию)
6. **Дополнительный вариант сортировки** - по цене покупки

---

## 📦 Изменения

### 1. Backend - UserCollectionController.cs

**Новый endpoint:**

```csharp
/// <summary>
/// Удалить все книги из коллекции
/// </summary>
[HttpDelete("all")]
public async Task<ActionResult> DeleteAllBooks()
{
    var userId = GetUserId();
    
    // Получаем все книги пользователя
    var books = await _usersContext.UserCollectionBooks
        .Where(b => b.UserId == userId)
        .ToListAsync();

    if (books.Count == 0)
    {
        return Ok(new { message = "Коллекция пуста", deletedCount = 0 });
    }

    var deletedCount = books.Count;

    // Удаляем изображения для каждой книги
    foreach (var book in books)
    {
        await _imageService.DeleteAllBookImagesAsync(userId, book.Id);
    }

    // Удаляем все книги
    _usersContext.UserCollectionBooks.RemoveRange(books);
    await _usersContext.SaveChangesAsync();

    return Ok(new { message = $"Удалено книг: {deletedCount}", deletedCount });
}
```

**Особенности:**
- Удаляет все книги пользователя
- Удаляет все изображения для каждой книги
- Каскадно удаляются связи (matches, images records)
- Возвращает количество удаленных книг
- Логирует операцию

---

### 2. Frontend - UserCollection.jsx

**Новые состояния:**

```jsx
const [sortBy, setSortBy] = useState('purchaseDate');     // По умолчанию - дата покупки
const [sortOrder, setSortOrder] = useState('desc');       // По убыванию
const [deleteAllDialogOpen, setDeleteAllDialogOpen] = useState(false);
const [deleting, setDeleting] = useState(false);
```

**Функция удаления:**

```jsx
const handleDeleteAll = async () => {
    setDeleting(true);
    try {
        const token = Cookies.get('token');
        await axios.delete(`${API_URL}/usercollection/all`, {
            headers: { Authorization: `Bearer ${token}` }
        });
        
        setDeleteAllDialogOpen(false);
        await loadCollection();
        await loadStatistics();
    } catch (err) {
        console.error('Error deleting all books:', err);
        setError('Не удалось удалить коллекцию');
    } finally {
        setDeleting(false);
    }
};
```

**Улучшенная логика сортировки:**

```jsx
const filteredAndSortedBooks = () => {
    let filtered = books;

    // Фильтрация...

    // Сортировка
    filtered = [...filtered].sort((a, b) => {
        let comparison = 0;
        
        switch (sortBy) {
            case 'addedDate': {
                const dateA = new Date(a.addedDate).getTime();
                const dateB = new Date(b.addedDate).getTime();
                comparison = dateB - dateA;
                break;
            }

            case 'purchaseDate': {
                const dateA = a.purchaseDate ? new Date(a.purchaseDate).getTime() : 0;
                const dateB = b.purchaseDate ? new Date(b.purchaseDate).getTime() : 0;
                comparison = dateB - dateA;
                break;
            }

            case 'title':
                comparison = (a.title || '').localeCompare(b.title || '');
                break;

            case 'price':
                comparison = (b.estimatedPrice || 0) - (a.estimatedPrice || 0);
                break;

            case 'purchasePrice':
                comparison = (b.purchasePrice || 0) - (a.purchasePrice || 0);
                break;
        }

        // Применяем направление сортировки
        return sortOrder === 'asc' ? -comparison : comparison;
    });

    return filtered;
};
```

**UI элементы:**

1. **Селект сортировки:**
```jsx
<TextField
    select
    label="Сортировка"
    value={sortBy}
    onChange={(e) => setSortBy(e.target.value)}
    size="small"
>
    <MenuItem value="purchaseDate">По дате покупки</MenuItem>
    <MenuItem value="addedDate">По дате добавления</MenuItem>
    <MenuItem value="title">По названию</MenuItem>
    <MenuItem value="price">По оценке</MenuItem>
    <MenuItem value="purchasePrice">По цене покупки</MenuItem>
</TextField>
```

2. **Селект направления:**
```jsx
<TextField
    select
    label="Порядок"
    value={sortOrder}
    onChange={(e) => setSortOrder(e.target.value)}
    size="small"
>
    <MenuItem value="desc">По убыванию</MenuItem>
    <MenuItem value="asc">По возрастанию</MenuItem>
</TextField>
```

3. **Кнопка удаления всех:**
```jsx
<Button
    variant="outlined"
    color="error"
    startIcon={<DeleteAllIcon />}
    onClick={() => setDeleteAllDialogOpen(true)}
    disabled={books.length === 0}
>
    <Box component="span" sx={{ display: { xs: 'none', sm: 'inline' } }}>Удалить </Box>всё
</Button>
```

4. **Диалог подтверждения:**
```jsx
<Dialog open={deleteAllDialogOpen} onClose={...}>
    <DialogTitle>Удалить всю коллекцию?</DialogTitle>
    <DialogContent>
        <Typography>Вы уверены, что хотите удалить все книги из коллекции?</Typography>
        <Typography color="error">Это действие необратимо! Будут удалены:</Typography>
        <Typography>• Все книги ({books.length} шт.)</Typography>
        <Typography>• Все изображения</Typography>
        <Typography>• Все связи с референсными книгами</Typography>
    </DialogContent>
    <DialogActions>
        <Button onClick={...}>Отмена</Button>
        <Button 
            onClick={handleDeleteAll}
            color="error"
            disabled={deleting}
        >
            {deleting ? 'Удаление...' : 'Удалить всё'}
        </Button>
    </DialogActions>
</Dialog>
```

---

## 🎨 Интерфейс

**Панель управления:**

```
┌───────────────────────────────────────────────────────────────┐
│  [Поиск...]  [Сортировка: По дате покупки ▼]  [Порядок: ↓ ▼] │
│                                                               │
│  [Добавить книгу]  [Импортировать]                           │
│  [PDF] [ZIP] [Удалить всё]                                    │
└───────────────────────────────────────────────────────────────┘
```

**Диалог удаления:**

```
┌─────────────────────────────────────┐
│  Удалить всю коллекцию?             │
│                                     │
│  Вы уверены?                        │
│                                     │
│  ⚠ Это действие необратимо!         │
│  Будут удалены:                     │
│  • Все книги (18 шт.)               │
│  • Все изображения                  │
│  • Все связи                        │
│                                     │
│  [Отмена]  [Удалить всё]            │
└─────────────────────────────────────┘
```

---

## 📊 Варианты сортировки

### 1. По дате покупки (по умолчанию)
- **По убыванию** (desc): Самые новые покупки вверху
- **По возрастанию** (asc): Самые старые покупки вверху

### 2. По дате добавления
- **По убыванию**: Недавно добавленные вверху
- **По возрастанию**: Давно добавленные вверху

### 3. По названию
- **По убыванию**: Я → А
- **По возрастанию**: А → Я

### 4. По оценочной стоимости
- **По убыванию**: Самые дорогие вверху
- **По возрастанию**: Самые дешевые вверху

### 5. По цене покупки
- **По убыванию**: Дорогие покупки вверху
- **По возрастанию**: Дешевые покупки вверху

---

## 🔒 Безопасность

### Удаление всех книг:

1. **Авторизация** - Требуется JWT токен
2. **Изоляция** - Удаляются только книги текущего пользователя
3. **Подтверждение** - Двойное подтверждение (диалог + кнопка)
4. **Предупреждение** - Показывается количество удаляемых книг
5. **Логирование** - Операция логируется на сервере
6. **Очистка** - Удаляются все связанные данные (изображения, matches)

### SQL запрос:

```sql
-- Получить книги пользователя
SELECT * FROM "UserCollectionBooks" 
WHERE "UserId" = @userId;

-- Удалить изображения
DELETE FROM "CollectionImages" 
WHERE "BookId" IN (SELECT "Id" FROM "UserCollectionBooks" WHERE "UserId" = @userId);

-- Удалить книги
DELETE FROM "UserCollectionBooks" 
WHERE "UserId" = @userId;
```

---

## ⚙️ Примеры использования

### Удаление всей коллекции

**HTTP запрос:**
```http
DELETE /api/usercollection/all
Authorization: Bearer {token}
```

**Ответ:**
```json
{
  "message": "Удалено книг: 18",
  "deletedCount": 18
}
```

### Сортировка

**По дате покупки (новые вверху):**
```javascript
sortBy = 'purchaseDate'
sortOrder = 'desc'
```

**По названию (А → Я):**
```javascript
sortBy = 'title'
sortOrder = 'asc'
```

**По цене покупки (дешевые вверху):**
```javascript
sortBy = 'purchasePrice'
sortOrder = 'asc'
```

---

## 🎯 Преимущества

✅ **Удобство** - Быстрое удаление всей коллекции  
✅ **Безопасность** - Подтверждение перед удалением  
✅ **Гибкость** - 5 вариантов сортировки + 2 направления = 10 комбинаций  
✅ **По умолчанию** - Разумная сортировка (по дате покупки, новые вверху)  
✅ **Очистка** - Полное удаление всех связанных данных  
✅ **Логирование** - Отслеживание критических операций  

---

## ✨ Готово!

**Теперь можно:**
- ✅ Удалить всю коллекцию одной кнопкой
- ✅ Сортировать по дате покупки по умолчанию
- ✅ Выбирать направление сортировки
- ✅ Сортировать по 5 различным критериям
- ✅ Видеть предупреждение перед удалением

**Функционал готов к использованию! 🚀**

