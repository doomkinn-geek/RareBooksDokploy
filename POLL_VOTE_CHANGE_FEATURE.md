# Возможность изменения голоса в голосованиях

## Описание изменений

Реализована возможность изменять свой голос в групповых чатах до момента завершения голосования создателем.

## Что было изменено

### Frontend (_may_messenger_mobile_app)

**Файл:** `lib/presentation/widgets/poll_widget.dart`

#### 1. Изменена логика отображения результатов

**Было:**
```dart
final showResults = hasVoted || isClosed;
```

**Стало:**
```dart
final showResults = isClosed;
```

Теперь результаты голосования (прогресс-бары с процентами) показываются **только когда голосование завершено**. До завершения пользователи видят чекбоксы/радио-кнопки и могут изменять свой выбор.

#### 2. Добавлено отображение количества голосов до завершения

Когда пользователь уже проголосовал, но голосование еще не завершено, показывается количество голосов рядом с каждой опцией (вместо процентов):

```dart
// Vote count hint (when poll is open and user has voted)
if (!isClosed && hasVoted)
  Text(
    '${option.voteCount}',
    style: TextStyle(
      fontSize: 12,
      color: primaryTextColor.withOpacity(0.6),
    ),
  ),
```

#### 3. Улучшена кнопка "Голосовать" для множественного выбора

- Добавлен метод `_hasChanges()` для определения, изменился ли выбор пользователя
- Кнопка меняет текст с "Голосовать" на "Изменить голос" после первого голосования
- Кнопка показывается только если есть реальные изменения в выборе

```dart
// Vote button for multiple choice
if (!isClosed && poll.allowMultipleAnswers && _selectedOptions.isNotEmpty && _hasChanges())
  ElevatedButton(
    child: Text(hasVoted ? 'Изменить голос' : 'Голосовать'),
  ),
```

#### 4. Добавлена подсказка для пользователей

После голосования в множественном выборе показывается подсказка:

```dart
if (!isClosed && hasVoted && poll.allowMultipleAnswers)
  Text(
    'Можно изменить выбор до завершения голосования',
    style: TextStyle(fontSize: 11, fontStyle: FontStyle.italic),
  ),
```

#### 5. Удалена кнопка "Отменить голос"

Кнопка больше не нужна, так как пользователь может напрямую изменить свой выбор, кликая на опции.

#### 6. Улучшена логика для одиночного выбора

При одиночном выборе (radio buttons) пользователь может сразу кликнуть на другую опцию, чтобы изменить свой голос. Голос отправляется автоматически.

### Backend (_may_messenger_backend)

**Изменений не требуется!** Бэкенд уже поддерживает изменение голоса:

**Файл:** `src/MayMessenger.API/Controllers/PollsController.cs`

Метод `Vote` (строки 130-226):
- Получает существующие голоса пользователя
- Удаляет старые голоса
- Добавляет новые голоса
- Корректно обновляет счетчики (не дублирует TotalVoters)
- Отправляет обновление через SignalR всем участникам чата

```csharp
// Get existing votes
var existingVotes = poll.Options
    .SelectMany(o => o.Votes)
    .Where(v => v.UserId == userId)
    .ToList();

// Remove old votes
foreach (var vote in existingVotes)
{
    var option = poll.Options.First(o => o.Id == vote.PollOptionId);
    option.VoteCount = Math.Max(0, option.VoteCount - 1);
    _context.Set<PollVote>().Remove(vote);
}

// Add new votes
foreach (var optionId in dto.OptionIds)
{
    var option = poll.Options.First(o => o.Id == optionId);
    option.VoteCount++;
    // ... создание нового голоса
}
```

## Как это работает

### Для пользователя

1. **Первое голосование:**
   - Одиночный выбор: клик на опцию → голос сразу отправляется
   - Множественный выбор: выбор опций → кнопка "Голосовать"

2. **Изменение голоса:**
   - Одиночный выбор: клик на другую опцию → голос автоматически меняется
   - Множественный выбор: изменение галочек → кнопка "Изменить голос" → подтверждение

3. **После завершения голосования:**
   - Чекбоксы/радио-кнопки скрываются
   - Показываются финальные результаты с процентами
   - Изменение голоса невозможно

### Визуальные отличия

**До завершения голосования:**
- ✅ Чекбоксы/радио-кнопки видны
- ✅ Можно менять выбор
- 🔢 Показывается количество голосов (не проценты)
- 💡 Подсказка "Можно изменить выбор..."

**После завершения голосования:**
- ❌ Чекбоксы/радио-кнопки скрыты
- ❌ Изменение невозможно
- 📊 Показываются прогресс-бары с процентами
- 🏁 Метка "Завершено"

## Преимущества

1. **Снижение ошибок:** Пользователи могут исправить случайно нажатый вариант
2. **Лучший UX:** Более гибкое взаимодействие с голосованием
3. **Прозрачность:** Видны текущие результаты (количество голосов), но можно изменить выбор
4. **Честность:** После завершения голосования изменения невозможны

## Техническая реализация

### Проверки безопасности (бэкенд)

```csharp
// Проверка, что голосование не закрыто
if (poll.IsClosed)
    return BadRequest("Poll is closed");

// Проверка срока действия
if (poll.ClosesAt.HasValue && poll.ClosesAt < DateTime.UtcNow)
{
    poll.IsClosed = true;
    await _context.SaveChangesAsync();
    return BadRequest("Poll has expired");
}

// Проверка членства в чате
var isMember = await _context.ChatParticipants
    .AnyAsync(cp => cp.ChatId == poll.Message.ChatId && cp.UserId == userId);
```

### Real-time обновления (SignalR)

После изменения голоса все участники чата получают обновление:

```csharp
await _hubContext.Clients.Group(poll.Message.ChatId.ToString())
    .SendAsync("PollUpdated", new { 
        MessageId = poll.MessageId, 
        Poll = pollDto 
    });
```

## Тестирование

### Сценарии для тестирования:

1. ✅ Создать голосование с одиночным выбором
2. ✅ Проголосовать за опцию A
3. ✅ Изменить голос на опцию B
4. ✅ Убедиться, что голос изменился (счетчики обновились)
5. ✅ Завершить голосование
6. ❌ Попытаться изменить голос (должно быть невозможно)

---

7. ✅ Создать голосование с множественным выбором
8. ✅ Выбрать опции A и B
9. ✅ Нажать "Голосовать"
10. ✅ Изменить выбор (снять B, добавить C)
11. ✅ Нажать "Изменить голос"
12. ✅ Убедиться, что голоса обновились
13. ✅ Завершить голосование
14. ❌ Попытаться изменить голос (должно быть невозможно)

## Совместимость

- ✅ Обратная совместимость с существующими голосованиями
- ✅ Работает с анонимными и публичными голосованиями
- ✅ Работает с одиночным и множественным выбором
- ✅ Все изменения происходят в real-time через SignalR

## Дата реализации

13 января 2026 года
